{
    "name": "cf_com_sap_bmi_scp_apps",
    "version": "1.0.0",
    "description": "Generated by cds init",
    "repository": "<Add your repository here>",
    "license": "UNLICENSED",
    "dependencies": {
        "@sap/cds": "^3.34.2",
        "@sap/xsenv": "^3.0.0",
        "@sap/xssec": "^2.2.5",
        "cf-nodejs-logging-support": "^6.4.3",
        "express": "^4.17.1",
        "passport": "^0.4.1",
        "semver": "^7.3.2",
        "uuid": "^8.3.1"
    },
    "devDependencies": {
        "@sap/cds-dk": "^3.0.0",
        "sqlite3": "^5"
    },
    "engines": {
        "node": "^10 || ^12"
    },
    "scripts": {
        "build": "cds build/all --clean",
        "deploy": "cds deploy",
        "start": "npx cds run",
        "install-dev": "(npm install); (cd instanceforms && npm install);"
    },
    "cds": {
        "requires": {
            "uaa": {
                "kind": "xsuaa"
            }
        },
        "auth": {
            "passport": {
                "strategy": "JWT"
            }
        },
        "build": {
            "target": "gen"
        }
    }
}
